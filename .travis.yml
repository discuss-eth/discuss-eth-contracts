language: node_js
node_js:
  - "node"
cache:
  directories:
    - node_modules
deploy:
  provider: script
  script: npm run migrate:kovan
  on:
    tags: true
    branches:
      only:
        - master